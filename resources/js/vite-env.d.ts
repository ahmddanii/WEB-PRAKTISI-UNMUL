/// <reference types="vite/client" />

import { Page } from '@inertiajs/core';
import { ReactElement, ReactNode } from 'react';

declare global {
  interface Window {
    // Custom window properties can go here
  }
}
